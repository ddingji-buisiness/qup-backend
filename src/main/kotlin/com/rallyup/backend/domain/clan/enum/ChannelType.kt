package com.rallyup.backend.domain.clan.enum

enum class ChannelType {
    GENERAL,        // 일반 채널
    GAME_SPECIFIC,  // 게임별 채널
    ANNOUNCEMENT,   // 공지 채널
    OFFICER_ONLY,   // 임원 전용
    MATCH_PLANNING  // 내전 계획
}